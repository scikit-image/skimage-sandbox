# TODO

# # Creating containers, listing them works well
# Figure out how do i write to the STDIN of some process which is waiting for
# input after we've created the container and launched it inside the same, also
# get the output back from STDOUT, all this via the REMOTE API.
